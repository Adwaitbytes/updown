{
  "name": "updown",
  "private": true,
  "version": "0.1.0",
  "description": "Telegram bot for sub-hour BTC binary options on Sui via DeepBook Predict",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:web": "cd packages/web && npm run build",
    "build:miniapp": "cd packages/miniapp && npm run build",
    "build:shared": "cd packages/shared && npm run build",
    "move:build": "cd move && sui move build",
    "move:test": "cd move && sui move test"
  },
  "workspaces": [
    "packages/*"
  ]
}
